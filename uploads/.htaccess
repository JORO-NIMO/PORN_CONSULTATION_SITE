Options -Indexes

# Disable PHP execution inside uploads
<FilesMatch "\.(php|phtml|phar)$">
    Require all denied
</FilesMatch>

# Force treat potentially executable text as plain text
AddType text/plain .php .phtml .phar .pl .cgi .sh .py

# Allow only common static assets
<IfModule mod_mime.c>
    AddType image/jpeg .jpg .jpeg
    AddType image/png .png
    AddType image/gif .gif
    AddType image/webp .webp
</IfModule>